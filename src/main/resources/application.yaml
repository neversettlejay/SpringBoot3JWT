jay-tech:
  application:
    security:
      secret-key:
      access-token-expiry-date:
      refresh-token-expiry-date:

server:
    port: 7465
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jwt_security_final
    username: dev
    password: Simform@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect